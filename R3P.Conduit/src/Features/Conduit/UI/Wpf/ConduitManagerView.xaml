<UserControl x:Class="R3P.Hivemind.Features.Conduit.UI.Wpf.ConduitManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="540" d:DesignHeight="400">
  <UserControl.Resources>
    <Color x:Key="BrandBlueColor">#FF185FAC</Color>
    <Color x:Key="DarkBackgroundColor">#FF0F141A</Color>
    <Color x:Key="CardBackgroundColor">#FF171E26</Color>
    <Color x:Key="DividerColor">#FF1F2A36</Color>
    <Color x:Key="ForegroundPrimaryColor">#FFF5F7FA</Color>
    <Color x:Key="ForegroundSecondaryColor">#FFB5C0CE</Color>

    <SolidColorBrush x:Key="BrandBlueBrush" Color="{StaticResource BrandBlueColor}" />
    <SolidColorBrush x:Key="DarkBackgroundBrush" Color="{StaticResource DarkBackgroundColor}" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}" />
    <SolidColorBrush x:Key="DividerBrush" Color="{StaticResource DividerColor}" />
    <SolidColorBrush x:Key="ForegroundPrimaryBrush" Color="{StaticResource ForegroundPrimaryColor}" />
    <SolidColorBrush x:Key="ForegroundSecondaryBrush" Color="{StaticResource ForegroundSecondaryColor}" />

    <Style x:Key="SectionHeaderText" TargetType="TextBlock">
      <Setter Property="FontSize" Value="16" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Foreground" Value="{StaticResource ForegroundPrimaryBrush}" />
      <Setter Property="Margin" Value="0,0,0,12" />
    </Style>

    <Style TargetType="Button">
      <Setter Property="Background" Value="{StaticResource BrandBlueBrush}" />
      <Setter Property="Foreground" Value="{StaticResource ForegroundPrimaryBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource BrandBlueBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="10,6" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Margin" Value="0,0,0,10" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="4">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Border" Property="Opacity" Value="0.5" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Border" Property="Background" Value="#FF1E6BDC" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Border" Property="Background" Value="#FF144E92" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
      <Setter Property="Foreground" Value="{StaticResource ForegroundPrimaryBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="6,3" />
      <Setter Property="Margin" Value="0,0,0,10" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="CaretBrush" Value="{StaticResource ForegroundPrimaryBrush}" />
    </Style>

    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{StaticResource ForegroundPrimaryBrush}" />
      <Setter Property="Margin" Value="0,6,0,10" />
    </Style>

    <Style TargetType="ListBox">
      <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
      <Setter Property="Foreground" Value="{StaticResource ForegroundPrimaryBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="6" />
    </Style>

    <Style x:Key="CardBorderStyle" TargetType="Border">
      <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
      <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="8" />
      <Setter Property="Padding" Value="18" />
    </Style>

    <Style x:Key="LabelText" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{StaticResource ForegroundSecondaryBrush}" />
      <Setter Property="Margin" Value="0,0,12,10" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
  </UserControl.Resources>

  <Grid Background="{StaticResource DarkBackgroundBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Border Background="#FF101620" Padding="18" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1">
      <DockPanel LastChildFill="True">
        <Border Width="72" Height="72" Background="{StaticResource BrandBlueBrush}" CornerRadius="12" Margin="0,0,18,0">
          <Image Source="pack://application:,,,/R3P.Hivemind;component/Assets/Root3powerLogo.png" Stretch="Uniform" />
        </Border>
        <StackPanel VerticalAlignment="Center">
          <TextBlock Text="Root3Power Conduit Manager" Foreground="{StaticResource ForegroundPrimaryBrush}" FontSize="20" FontWeight="Bold" />
          <TextBlock Text="Measure | Route | Report" Foreground="{StaticResource ForegroundSecondaryBrush}" FontSize="12" />
        </StackPanel>
      </DockPanel>
    </Border>

    <Grid Grid.Row="1" Margin="18">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Border Grid.Column="0" Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBlock Text="Conduit Settings" Style="{StaticResource SectionHeaderText}" />
          <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Tag Prefix" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbPrefix" Grid.Column="1" />

            <TextBlock Text="Next #" Grid.Row="1" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbNext" Grid.Row="1" Grid.Column="1" />

            <TextBlock Text="Allowance %" Grid.Row="2" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbAllow" Grid.Row="2" Grid.Column="1" />

            <TextBlock Text="Round Increment" Grid.Row="3" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbRound" Grid.Row="3" Grid.Column="1" />

            <TextBlock Text="Text Height" Grid.Row="4" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbText" Grid.Row="4" Grid.Column="1" />

            <TextBlock Text="Obstacle Layers" Grid.Row="5" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbLayers" Grid.Row="5" Grid.Column="1" />

            <TextBlock Text="Clearance" Grid.Row="6" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbClearance" Grid.Row="6" Grid.Column="1" />

            <TextBlock Text="Grid Step" Grid.Row="7" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbGrid" Grid.Row="7" Grid.Column="1" />

            <CheckBox x:Name="CbAllow45" Grid.Row="8" Grid.ColumnSpan="2" Content="Allow 45 deg routing" />

            <TextBlock Text="Fiber Threshold (ft)" Grid.Row="9" Style="{StaticResource LabelText}" />
            <TextBox x:Name="TbFiber" Grid.Row="9" Grid.Column="1" />
          </Grid>
        </Grid>
      </Border>

      <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource CardBorderStyle}" Margin="12,0,0,0">
        <StackPanel>
          <TextBlock Text="Actions" Style="{StaticResource SectionHeaderText}" />
          <Button x:Name="BtnSave" Content="Save" />
          <Button x:Name="BtnMeasureAdd" Content="Measure + Add" />
          <Button x:Name="BtnRoute2" Content="Route 2 Points" />
          <Button x:Name="BtnPlace" Content="Place Tag for Selected" />
          <Button x:Name="BtnRemove" Content="Remove Selected" />
          <Button x:Name="BtnRefresh" Content="Refresh" />
          <Separator Margin="0,6" Background="{StaticResource DividerBrush}" />
          <Button x:Name="BtnTable" Content="Insert Table" />
          <Button x:Name="BtnCsv" Content="Export CSV" />
          <Button x:Name="BtnExcel" Content="Export Excel" />
        </StackPanel>
      </Border>

      <Border Grid.ColumnSpan="2" Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="18" Margin="0,12,0,0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>
          <TextBlock Text="Measured Items" Style="{StaticResource SectionHeaderText}" />
          <ListBox x:Name="LbItems" Grid.Row="1" />
        </Grid>
      </Border>
    </Grid>
  </Grid>
</UserControl>







